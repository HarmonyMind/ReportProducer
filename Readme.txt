npm init -y
npm install -S express ejs puppeteer nock axios @faker-js/faker


if needed to set up custom port for this service:
export PORT=4401

if needed to ser up custom URL for collector service:
export COLLECTOR_URL="http://localhost:1991"
